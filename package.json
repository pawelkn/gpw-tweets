{
  "name": "volume-notifier",
  "version": "1.0.3",
  "description": "Volume Notifier sends email with list of stocks, which volume increased significantly since last trading day",
  "prepare": "npm run build",
  "main": "build/volume-notifier.js",
  "types": "build/volume-notifier.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node build/volume-notifier.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "stock",
    "gpw",
    "volume"
  ],
  "author": "Paweł Knioła",
  "license": "MIT",
  "dependencies": {
    "etl": "^0.7.0",
    "nodemailer": "^6.7.5",
    "unzipper": "^0.10.11"
  },
  "devDependencies": {
    "@types/node": "^17.0.8",
    "@types/nodemailer": "^6.4.4",
    "@types/unzipper": "^0.10.5",
    "typescript": "^3.9.10"
  }
}